@model TicketHunter.Models.ChartViewModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/Content/chart.css" rel="stylesheet" />
    @Styles.Render("~/Content/styles")
    <title>Chart</title>
</head>
<body>
    @Html.Hidden("Ticket",(Int32)@ViewBag.TicketId)
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-2">
                <p class="alert-info">@Model.Ticket.Title</p>
                <p class="alert-info">@Model.Ticket.Price</p>
            </div>
            <div class="col-md-10">
                <p class="alert-info fs50 fff">@Model.Ticket.Price</p>
                <div id="chart"></div>
            </div>
        </div>
    </div>
    <link rel="shortcut icon" href="~/Content/img/tickets_icon.ico">
    <script src="http://code.jquery.com/jquery-latest.min.js"
            type="text/javascript"></script>
    <script type="text/javascript" src="https://app.seats.io/chart.js"></script>
    <script src="~/Scripts/Admin/myChart.js"></script>
</body>
</html>
